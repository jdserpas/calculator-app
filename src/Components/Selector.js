import React from "react";

const Selector = () => {
  return(
    <>
      <select data-testid="drop-down">
        <option value="+">+</option>
      </select>
    </>
  );
};

export default Selector;